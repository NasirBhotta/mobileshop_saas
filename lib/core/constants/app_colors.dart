import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // ─── Primary (Deep Indigo - Trust + Professional) ───
  static const Color primary = Color(0xFF3D5AFE); // Indigo
  static const Color primaryLight = Color(0xFF8187FF);
  static const Color primaryDark = Color(0xFF0031CA);

  // ─── Secondary (Teal - Modern + Fresh) ───
  static const Color secondary = Color(0xFF00BFA5);
  static const Color secondaryLight = Color(0xFF5DF2D6);
  static const Color secondaryDark = Color(0xFF008E76);

  // ─── Backgrounds ───
  static const Color background = Color(0xFFF5F6FA); // off-white
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEEF0F8); // cards

  // ─── Text ───
  static const Color textPrimary = Color(0xFF1A1D2E); // near black
  static const Color textSecondary = Color(0xFF6B7280); // gray
  static const Color textHint = Color(0xFFB0B7C3);

  // ─── Semantic Colors ───
  static const Color success = Color(0xFF10B981); // sale complete ✅
  static const Color warning = Color(0xFFF59E0B); // low stock ⚠️
  static const Color error = Color(0xFFEF4444); // payment fail ❌
  static const Color info = Color(0xFF3B82F6); // info ℹ️

  // ─── Border / Divider ───
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}
